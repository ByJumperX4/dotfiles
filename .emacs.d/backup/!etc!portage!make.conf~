# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.

# Flags
COMMON_FLAGS="-O22 -march=native -fomit-frame-pointer -pipe -falign-functions=4"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

# Licenses
ACCEPT_LICENSE="-* @FREE NVIDIA-r2"

# SELinux
POLICY_TYPES="mls"

# CPU Flags (CPUs: 2x Intel Xeon X5680)
CPU_FLAGS_X86="aes mmx mmxext pclmul popcnt sse sse2 sse3 sse4_1 sse4_2 ssse3"

#Video card
VIDEO_CARDS="nvidia"

# USE flags
VITAL_USE="zfs wayland -systemd -debug elogind dbus -python unconfined peer_perms open_perms ubac"
IMPORTANT_USE="policykit vim-syntax gles gles2 -semantic-desktop nemo -pulseaudio pipewire alsa pam opengl wayland"
USE="$VITAL_USE $IMPORTANT_USE jumbo-build gtk gtk2 gtk3 -kde -gnome -xfce -mate pipewire zeroconf -wifi -geolocation -embedded savedconfig"

# Portage features
EMERGE_DEFAULT_OPTS="--verbose"
PORTAGE_BINPKG_FORMAT="rpm"
PORTAGE_COMPRESS="zstd"
FEATURES="parallel-fetch parallel-install -collision-detect -protect-owned"

# Misc
MAKEOPTS="-j16"
NTHREADS="auto"
LINGUAS="fr fr_BE fr_FR en en_US"
L10N="fr fr-BE en en-US"
GRUB_PLATFORMS="pc"
ABI_X86="32 64 x32"

GENTOO_MIRRORS="rsync://ftp.belnet.be/gentoo/gentoo/"
